- defaults:
    link:
      relink: true

- clean: ['~']


- link:
    ~/.gitconfig:
    ~/.zshenv:
    ~/.zshrc:
    ~/.config:
      glob: true
      create: true
      path: config/*

- shell:
  -
    command: ./setup_homebrew.zsh
    stdout: true
    stderr: true
